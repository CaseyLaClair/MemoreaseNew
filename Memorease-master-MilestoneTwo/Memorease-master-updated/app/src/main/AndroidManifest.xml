<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.memorease.laclair.android.myapplication">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateCardActivity" />
        <activity android:name=".AllTopicsActivity" />
        <activity android:name=".TodaysStudyActivity" />
        <activity android:name=".MyScoresActivity" />
        <activity android:name=".TopicScoreActivity" />
        <activity android:name=".CreateNewTopic" />
        <activity android:name=".SingleTopic" />
        <activity android:name=".StudyCards"></activity>
    </application>

</manifest>